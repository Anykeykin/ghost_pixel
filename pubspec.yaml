name: ghost_pixel
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.1.5

dependencies:
  image: ^3.2.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
